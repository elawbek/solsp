# Rust build artifacts
/target
**/target
/dist

# Local design docs / specs — kept out of VCS per project preference
/docs/

# Workflow-plugin plan/audit runs — local working state, not versioned
/.workflow/

# Local Claude Code tooling (hooks, agents, skills, settings) — not versioned
/.claude/

# Local Codex project skills — installed for this checkout, not versioned
/.codex-local/

# Local VS Code launch/debug helpers and scratch TODOs
/.vscode/
/TODO.txt

# Agent-memory plugin store (SQLite db + embeddings/graph/index/markdown mirrors)
# — canonical durable store, kept local, not versioned
/.memory/

# Editor / OS noise
*.swp
.DS_Store
